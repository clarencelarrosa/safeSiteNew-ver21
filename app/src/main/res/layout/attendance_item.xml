<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:descendantFocusability="blocksDescendants"
    android:orientation="horizontal"
    android:layout_gravity="center"
    app:flow_verticalAlign="center">

    <CheckBox
        android:id="@+id/attendance_check_box"
        android:layout_width="wrap_content"
        android:layout_alignParentStart="true"
        android:layout_height="wrap_content" />

    <TextView
        android:id="@+id/attendance_name"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_centerInParent="true"
        android:layout_toEndOf="@id/attendance_check_box"
        android:text="@string/test_name_product"
        android:textColor="@color/white"
        android:textSize="20sp" />

    <!--
    <TextView
        android:id="@+id/product_time"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_alignParentEnd="true"
        android:layout_centerInParent="true"
        android:layout_toEndOf="@id/attendance_name"
        android:gravity="end"
        android:text="@string/test_attendance_time"
        android:textStyle="italic" />
        -->
</RelativeLayout>